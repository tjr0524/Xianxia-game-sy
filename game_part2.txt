=0&&!M.events.foundationInsight;u.btCost.textContent=bt.init?`${bt.s} 영석 · ${bt.h} 영초 · 입문 즉시 전투/수련 개방`:`${bt.s} 영석 · ${bt.h} 영초${eventNeed?' · 축기 기연 필요':''}`;u.bt.disabled=ph==='run'||eventNeed;u.eventInfo.style.display=bt.major?'block':'none';if(bt.major&&M.realm.major===0)u.eventInfo.innerHTML=M.events.foundationInsight?'✓ <b>축기 기연 확보</b><br>적혈비경 수호영맥을 돌파하고 무사 귀환했습니다.':'<b>특수 조건: 축기 기연</b><br>연기 9층에서 적혈비경 정예 수호수를 격파한 뒤 출구로 무사 귀환해야 합니다.';renderAreas();renderTree();save()} function renderAreas(){u.areas.innerHTML='';for(const a of AREAS){let unlocked=!!M.unlocked[a.id],ready=areaReady(a),active=a.id===M.area,b=document.createElement('button');b.className='areaBtn'+(active?' active':'')+(!unlocked&&!ready?' locked':'')+(ready&&!unlocked?' ready':'');let status=unlocked?`개척 ${treeCount(a.id)}/9 · 정예 ${M.zones[a.id].eliteWins}회`:ready?'개방 가능':`🔒 ${MAJORS[a.req.major]} ${a.req.stage}층 · 이전 비경 트리 ${a.req.nodes}노드`;b.innerHTML=`<div class="row"><b>${active?'▶ ':''}${a.name}</b><span class="sm">${a.rec}</span></div><div class="sm">${status}</div>`;b.disabled=ph==='run';b.onclick=()=>{if(ph==='run')return;if(!unlocked&&!unlockArea(a)){u.notice.textContent='아직 이 비경을 개방할 조건이 부족합니다.';return}M.area=a.id;u.start.textContent='비경 진입';u.ox.innerHTML=`<b>${a.name}</b><br>${a.desc}<br>이 비경의 인연 트리는 별도로 보존됩니다.`;u.notice.textContent=`${a.name}을 선택했습니다.`;ref();draw()};u.areas.appendChild(b)}} function renderTree(){u.tree.innerHTML='';const heads={eco:['생태','🩸'],res:['자원','⛏️'],fate:['인물·기연','✦']};for(const key of ['eco','res','fate']){let br=document.createElement('div');br.className='branch';br.innerHTML=`<div class="branchTitle">${heads[key][1]} ${heads[key][0]}</div>`;for(const n of TREE[key]){let on=treeOn(n.id),ready=nodeReady(n),q=nodeCost(n),req=nodeReq(n),b=document.createElement('button');b.className='treeNode'+(on?' on':ready?' available':' locked')+(n.core?' core':'');let reqText=on?'활성화됨':!meetsRealm(req)?`${MAJORS[req.major]} ${req.stage}층 필요`:n.p&&!treeOn(n.p)?'이전 노드 필요':`${q.s?`${q.s}석 `:''}${q.h?`${q.h}초`:''}`;b.innerHTML=`<span class="nodeName">${n.n}</span><span class="nodeCost">${reqText}</span>`;b.disabled=ph==='run'||on||!ready;b.onclick=()=>unlockNode(n.id);b.title=n.d;br.appendChild(b)}u.tree.appendChild(br)}u.nc.textContent=`${treeCount()} / 9`;u.treeHint.textContent=isMortal()?'수선에 입문하면 인연 트리가 열립니다.':'노드를 눌러 해금 · 강해진 만큼 비경의 위험과 수익을 올리세요.'} function unlockNode(id){if(ph==='run')return;let n=nodeBy(id);if(!n||treeOn(id))return;if(!nodeReady(n)){u.notice.textContent='경지 또는 이전 인연 노드 조건이 부족합니다.';return}let q=nodeCost(n);if(M.stone<q.s||M.herb<q.h){u.notice.textContent='인연 해금 자원이 부족합니다.';return}M.stone-=q.s;M.herb-=q.h;Z().tree[id]=1;u.notice.textContent=`${n.n} 해금 — ${n.d}`;ref()} function pt(m=30){return{x:m+Math.random()*(700-2*m),y:m+Math.random()*(460-2*m)}}function ep(){let e=Math.floor(Math.random()*3),m=22;return e===0?{x:m,y:60+Math.random()*340}:e===1?{x:700-m,y:60+Math.random()*340}:{x:40+Math.random()*620,y:m}}function drop(type,x,y,val=1){O.push({type,x,y,r:type==='s'?7:8,val})}function rndDrop(){let p=pt();drop('h',p.x,p.y,1)}function pop(x,y,s,col='#fff',life=.9){F.push({x,y,s,col,t:life,ttl:life})} const TB=[{id:'wind',n:'풍행부',mark:'風',desc:'이동속도 +22%'},{id:'ward',n:'호신옥',mark:'護',desc:'받는 피해 -30%'},{id:'gather',n:'취령주',mark:'聚',desc:'영석 획득 +50%'},{id:'blade',n:'비검편',mark:'劍',desc:'공격력 +25%'}]; function actor(type,opt={}){let p=opt.p||ep(),d=A().enemy,base=type==='elite'?300:type==='spirit'?1:type==='rogue'?55:type==='rat'?30:type==='chaser'?72:84,hp=base*d,e={type,x:p.x,y:p.y,r:type==='elite'?23:type==='spirit'?10:type==='rat'?9:12,hp,max:hp,cd:0,ag:0,hx:opt.hx??p.x,hy:opt.hy??p.y,hs:0,hh:0,esc:0,bond:0,vx:(Math.random()-.5)*45,vy:(Math.random()-.5)*45,treasure:null,rare:0};e.sp=type==='chaser'?48:type==='guard'?36:type==='elite'?24:type==='rogue'?56:type==='rat'?74:58;if((type==='guard'||type==='chaser')&&treeOn('eco3')&&Math.random()<.22){e.rare=1;e.hp*=1.85;e.max=e.hp;e.r+=3;e.sp*=1.08}if(type==='elite'&&treeOn('res3')){e.hp*=1.55;e.max=e.hp;e.r+=3}if(type==='rogue'){let chance=treeOn('fate2')?.94:.55;if(Math.random()<chance){e.treasure=TB[Math.floor(Math.random()*TB.length)];if(e.treasure.id==='wind')e.sp*=1.25;if(e.treasure.id==='ward'){e.hp*=1.5;e.max=e.hp}}}E.push(e);return e} function setupMine(){let p={x:150+Math.random()*400,y:105+Math.random()*210},aa=Math.random()*6.28,ex=p.x+Math.cos(aa)*72,ey=p.y+Math.sin(aa)*72;V={x:p.x,y:p.y,r:19,stock:(treeOn('res3')?44:24)+areaIndex()*12,prog:0,alert:0,cleared:0};let e=actor('elite',{p:{x:ex,y:ey},hx:ex,hy:ey});e.mineGuard=1;e.mineX=p.x;e.mineY=p.y;pop(p.x,p.y,'수호 영맥 발견','#b9d7ff',1.6)} function begin(){ph='run';t=0;ns=4.8;scd=0;E=[];O=[];F=[];V=null;let herbTotal=10+(treeOn('res1')?4:0),herbInitial=Math.ceil(herbTotal*.78),beastTotal=(isMortal()?3:4)+(treeOn('eco1')?2:0)+(treeOn('eco2')?3:0),beastInitial=Math.ceil(beastTotal*.76);R={s:0,h:0,left:0,buffs:{},elite:0,herbSpawnLeft:herbTotal-herbInitial,beastSpawnLeft:beastTotal-beastInitial};nd=7.5+Math.random()*3.5;nb=8+Math.random()*4;nsp=999;P.x=P.tx=EXIT.x;P.y=P.ty=EXIT.y;P.T=null;P.max=isMortal()?24:Math.floor((90+(M.cult.hp-1)*25)*realmPower());P.hp=P.max;P.cd=0;u.ov.classList.add('hide');u.ret.disabled=false;u.skill.disabled=isMortal();u.notice.textContent=isMortal()?'범인은 전투할 수 없습니다. 입장 시 배치된 영초를 보고 안전한 루트를 고르세요.':'입장 시 보이는 요수·영초 배치를 보고 먼저 루트를 정하세요.';for(let i=0;i<herbInitial;i++)rndDrop();for(let i=0;i<beastInitial;i++)actor(Math.random()<.62?'guard':'chaser');if(treeOn('fate3'))actor('spirit');if(treeOn('res2'))setupMine();ref()} function end(reason){if(ph!=='run')return;ph='home';P.T=null;let ratio=reason==='return'?1:.4,s=Math.floor(R.s*ratio),h=Math.floor(R.h*ratio),event='';M.stone+=s;M.herb+=h;Z().runs++;if(reason==='return')Z().safe++;if(R.elite)Z().eliteWins++;if(reason==='return'&&M.area==='blood'&&R.elite&&M.realm.major===0&&M.realm.stage===9&&!M.events.foundationInsight){M.events.foundationInsight=1;event='<div class="event"><b>기연 발생 · 축기의 실마리</b><br>혈갑폭웅을 쓰러뜨리고 영맥의 기운을 품은 채 귀환했습니다.<br><b>축기 기연을 확보했습니다.</b></div>'}u.skill.disabled=u